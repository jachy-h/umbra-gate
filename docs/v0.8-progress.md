# v0.8 开发进展

> 本文档随实现同步更新。

## 当前状态

- **已完成**：功能开发与自动化验证全部完成；手工验收按指示暂定通过（2026-09-04）。

## 实施清单

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| 数据库 schema migration | 已完成 | 事务迁移旧 `fallback_model` 为有序 `model_priorities_json`，并永久删除 Link 级 `api_key` 列。 |
| Go 模型、存储层与管理 API | 已完成 | 增加模型优先级校验、禁止 `chain[].api_key`，并提供 `PUT /admin/providers/:id/api-key`。 |
| 路由与链路测试 | 已完成 | 每个节点按优先级尝试模型、同模型去重、同模型重试；链路测试支持可选 `model`。 |
| 前端类型与 API client | 已完成 | 使用 `model_priorities`，接入 Provider API Key 专用写入与可选链路测试模型参数。 |
| Link 编辑器与 Provider 密钥 Modal | 已完成 | 移除协议/API 格式提示；Provider 与模型优先级左右排列；已有请求模型时隐藏添加入口；固定模型提供 Provider 模型候选；密钥 Modal 写入 Provider。 |
| Provider 管理页与文案 | 已完成 | Provider 密钥修改使用专用接口，列表仅展示设置状态；中英文文案已同步。 |
| 全量验证 | 已完成 | 自动化 Go 测试、前端 lint 与构建均已通过；手工验收暂定通过（2026-09-04）。 |

## 验证记录

- `go test ./internal/api ./internal/proxy ./internal/server ./internal/db`：通过。
- LSP（12 个本次修改的 Go 文件）：0 个诊断。
- `cd web && npm run lint`：通过。
- `cd web && npm run build`：通过。
- `go test ./...`：通过（含 Provider 模型刷新接口的认证、去重与持久化测试）。
- TypeScript LSP：无错误（存在既有复杂度/弃用 API 提示）。
- 2026-08-11：按反馈移除协议/API 格式提示，将 Provider 与对应模型优先级调整为左右布局，并在已有请求模型时隐藏添加按钮；Provider API Key 状态与操作按钮改为分行显示。
- 2026-08-11：Link 未保存时切换顶部菜单或取消编辑会先确认；配置密钥成功后不再显示修改 Provider API Key 入口；所有 API Key 输入框禁用浏览器密码自动填充。
- 2026-08-11：移除已配置 Provider 的 API Key 状态文案；在 Link 编辑器的 Provider 下拉选项中，以绿色勾选图标标记已配置 API Key 的 Provider。
- 2026-08-11：固定模型改为可搜索选择；选项来自 Provider 配置的模型目录，并允许输入不在目录中的自定义模型。
- 2026-08-11：排查确认固定模型选择未调用远程模型接口；当前仅读取已加载的 `Provider.models`，管理 API 尚未实现远程模型发现/刷新端点。
- 2026-08-11：新增 `POST /admin/providers/:id/models/refresh`：服务端以 Provider 的 Base URL/API Key 请求 OpenAI 兼容的 `/models`，去重保存 `data[].id`；Provider 管理页提供手动刷新，固定模型选择器首次打开时自动刷新当前 Provider。
- 2026-08-11：选择或更换 Provider 时立即刷新其模型目录；刷新成功后清空新 Provider 目录中不存在的固定模型；Chain Preview 重构为按节点纵向展示的回退路径及模型标签。
- 2026-08-11：选择器输入框和选项均增加完整名称悬停提示；节点布局将 Retry 移至 Provider 下方，并将模型优先级区域从等宽列扩展为 3/5 宽度。
- 2026-08-11：确认 DeepSeek 模型刷新请求为 `GET https://api.deepseek.com/models`，并在已配置密钥时发送 `Authorization: Bearer <api_key>`；401 表示凭据缺失、无效或无权访问，而非模型接口 URL 错误。
- 2026-08-11：移除模型刷新失败后的缓存兜底：刷新失败即清空持久化及当前页面中的模型目录；DeepSeek 不再预置静态模型，并在启动时清理旧版 `deepseek-chat`/`deepseek-reasoner` 种子目录。

## 待手工验收

（暂定通过，2026-09-04）

- 在 Link 编辑器中添加、删除和排序“接收到的模型”及固定模型，确认保存后的顺序保持不变。
- 为未配置密钥的 Provider 打开配置 Modal，保存后确认当前 Link 表单未重置且状态立即更新。
- 从 Provider 管理页修改密钥，确认列表始终只显示掩码状态。
- 在 Link 测试 Modal 分别传入和不传入测试模型，确认请求模型项仅在提供测试模型时参与测试。

## v0.8.0 发布前收尾（2026-09-04）

- 手工验收按指示暂定通过。
- 最近请求列表现在包含链路测试记录并显示「链接测试」标记（原行为为排除链路测试记录）。
- 新增预置 OpenRouter Provider（OpenAI/chat_completions 端点，模型刷新请求 `GET https://openrouter.ai/api/v1/models?limit=500`）；Provider 管理页与 Link 编辑器可通过 `POST /admin/providers/:id/models/refresh` 加载模型目录。
